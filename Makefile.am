# Makefile.am

ACLOCAL_AMFLAGS = -I m4
SUBDIRS         = libltdl plugin tests

maintainer-clean-local:
	-rm -rf m4 libltdl
	-rm -f Makefile.in aclocal.m4 config.guess config.h.in config.sub
	-rm -f ar-lib compile configure depcomp install-sh ltmain.sh missing
	-rm -f depcomp test-driver
